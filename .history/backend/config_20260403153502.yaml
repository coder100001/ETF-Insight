server:
  host: "0.0.0.0"
  port: 8080
  read_timeout: 30s
  write_timeout: 30s

database:
  dsn: "etf_insight.db"

etf:
  cache:
    type: "memory"
    ttl: 3600

schedule:
  update_interval: "1h"

log:
  level: "info"
